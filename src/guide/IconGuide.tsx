import * as React from 'react';
import Section from './Section';
import { Icon } from '../components';

const IconGuide = () => (
  <Section title="Icon" description="An icon.">
    <Icon className="icon-chevron-right" />
  </Section>
);

export default IconGuide;
