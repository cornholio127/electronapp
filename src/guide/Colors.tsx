import * as React from 'react';
import Section from './Section';

const Colors = () => (
  <Section title="Colors" description="Default colors.">
    Some colors
  </Section>
);

export default Colors;
