import * as React from 'react';
import Section from './Section';
import { Switch } from '../components';

const SwitchGuide = () => (
  <Section title="Switch" description="A switch.">
    <Switch />
  </Section>
);

export default SwitchGuide;
